window.NotificationsCenter={async render(){let rows=[];try{rows=await req('/notifications')}catch{}return `<div class="card"><div class="section-head"><div><h2>Уведомления</h2><p>Тренировки, заявки, сообщения и события Федерации</p></div></div><div class="list">${rows.map(n=>`<div class="list-item"><span><b>${esc(n.title||'Уведомление')}</b><br><span class="muted">${esc(n.body||n.text||'')}</span></span><span class="pill ${n.read?'':'warn'}">${n.read?'Прочитано':'Новое'}</span></div>`).join('')||'<div class="empty">Новых уведомлений нет</div>'}</div></div>`}};