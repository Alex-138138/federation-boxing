window.AdminDirectory={
 async render(hostId='adminDirectory'){const host=document.getElementById(hostId);if(!host)return;try{const [halls,groups]=await Promise.all([req('/directory/halls'),req('/directory/groups')]);host.innerHTML=`<div class="grid"><div class="card col-6"><h3>Залы</h3>${(halls||[]).map(x=>`<div class="list-item"><div><b>${x.name||'Зал'}</b><div class="muted">${x.address||''}</div></div></div>`).join('')||'<div class="empty">Нет залов</div>'}</div><div class="card col-6"><h3>Группы</h3>${(groups||[]).map(x=>`<div class="list-item"><div><b>${x.name||'Группа'}</b><div class="muted">${x.join_code||''}</div></div></div>`).join('')||'<div class="empty">Нет групп</div>'}</div></div>`}catch(e){host.innerHTML='<div class="empty">Не удалось загрузить справочники</div>'}}
};