window.MessagesCenter={async render(){let rows=[];try{rows=await req('/messages')}catch{}return `<div class="grid"><div class="card col-8"><h2>Сообщения</h2><div class="list">${rows.map(m=>`<div class="list-item"><span><b>${esc(m.sender_name||'Сообщение')}</b><br><span>${esc(m.text||m.body||'')}</span><br><span class="muted">${esc(m.created_at||'')}</span></span></div>`).join('')||'<div class="empty">Сообщений пока нет</div>'}</div></div><div class="card col-4"><h3>Правила общения</h3><p class="muted">Тренер пишет только своим группам. Сообщения группы доступны спортсменам и их родителям. Администратор имеет доступ к журналу сообщений.</p></div></div>`}};