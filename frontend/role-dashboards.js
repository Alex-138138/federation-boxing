window.RoleDashboards={schedule(items=[]){return items.length?items.map(x=>`<div class="schedule-row"><b>${esc(String(x.weekday))}</b><span>${esc(x.start_time)}–${esc(x.end_time)}</span><span class="pill">Тренировка</span></div>`).join(''):'<div class="empty">Расписание пока не заполнено</div>'},achievements(items=[]){return items.length?items.map(x=>`<div class="list-item"><div><b>${esc(x.title)}</b><div class="muted">${esc(x.description||'')}</div></div><span class="pill gold">+${x.points||0}</span></div>`).join(''):'<div class="empty">Достижений пока нет</div>'},attendance(items=[]){return items.length?items.map(x=>`<div class="list-item"><span>${esc(x.training_date)}</span><span class="pill ${x.status==='present'?'ok':'warn'}">${esc(x.status)}</span></div>`).join(''):'<div class="empty">Посещаемость пока не отмечена</div>'},rating(points=0){return `<div class="rating-ring"><strong>${Number(points)||0}</strong></div><div class="muted center">Персональный рейтинг спортсмена</div>`}}
